# ----------------------------------------------------
# NLWeb RAG API Environment Variables
# ----------------------------------------------------
# Copy this file to .env and fill in your actual credentials.

# --- OpenAI Configuration ---
# Your API key for OpenAI services (LLM and Embeddings)
OPENAI_API_KEY="sk-your_openai_api_key_here"

# --- Vector Database (PostgreSQL) ---
# The full connection string for your PostgreSQL vector database.
# Format: "postgresql://<user>:<password>@<host>:<port>/<database>"
POSTGRES_CONNECTION_STRING="postgresql://sitetor_master:temp1234@localhost:5433/sitetor_vector_db"

# The password for your PostgreSQL vector database.
# This is also referenced by 'api_key_env' in config_retrieval.yaml.
POSTGRES_PASSWORD="temp1234"

# --- Main Application Database (for Multi-Tenancy) ---
# This database stores site information and API keys.
# It can be a separate PostgreSQL instance or a different database in the same instance.
MAIN_DB_HOST="localhost"
MAIN_DB_NAME="sitetor_main_db"
MAIN_DB_USER="sitetor_master"
MAIN_DB_PASSWORD="temp1234"

# --- OAuth Configuration (Optional) ---
# Required if you want to test the authenticated chat interface.
# Generate a random secret key for session management.
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
OAUTH_SESSION_SECRET="your_randomly_generated_session_secret"

# GitHub OAuth App credentials
GITHUB_CLIENT_ID="your_github_oauth_client_id"
GITHUB_CLIENT_SECRET="your_github_oauth_client_secret"
